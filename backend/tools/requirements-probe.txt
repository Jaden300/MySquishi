# Dependencies for the hardware bring up probe.
#
# As of Phase 3 pyserial is also a real dependency of the application, declared
# in backend/requirements.txt, because app/sources/serial_source.py reads the
# live sensor. Installing the app requirements is therefore enough to run the
# probe too, and this file is kept so the probe still declares what it needs
# rather than relying on that overlap.
#
#     backend/.venv/bin/pip install -r tools/requirements-probe.txt
#
# Guard tests still assert that serial_source.py is the only module under app/
# that imports pyserial, so the hardware boundary stays a boundary.
pyserial
